[build]
builder = "RAILPACK"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python manage.py migrate && python populate_db.py && gunicorn multitask_backend.wsgi --bind 0.0.0.0:$PORT --log-file -"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10
